@extends('site.layout.site-app')

@section('title', 'S\'abonner à ' . $plan->name)

@section('content')
<div class="subscribe-page">
    <!-- Hero Section -->
    <section class="hero-section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-10 text-center">
                    <div class="hero-content">
                        <div class="hero-badge">
                            <i class="ti ti-star"></i>
                            <span>Plan Premium</span>
                        </div>
                        <h1 class="hero-title">
                            Débloquez l'<span class="highlight">Accès Premium</span>
                        </h1>
                        <p class="hero-subtitle">
                            Rejoignez notre communauté exclusive et accédez à du contenu de qualité supérieure
                        </p>
                        <div class="hero-stats">
                            <div class="stat-item">
                                <div class="stat-number">10K+</div>
                                <div class="stat-label">Membres Actifs</div>
                            </div>
                            <div class="stat-divider"></div>
                            <div class="stat-item">
                                <div class="stat-number">24/7</div>
                                <div class="stat-label">Support</div>
                            </div>
                            <div class="stat-divider"></div>
                            <div class="stat-item">
                                <div class="stat-number">99%</div>
                                <div class="stat-label">Satisfaction</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Floating Elements -->
        <div class="floating-elements">
            <div class="floating-card floating-card-1">
                <i class="ti ti-crown"></i>
            </div>
            <div class="floating-card floating-card-2">
                <i class="ti ti-rocket"></i>
            </div>
            <div class="floating-card floating-card-3">
                                                        <i class="fas fa-bolt"></i>
            </div>
        </div>
    </section>

    <!-- Subscription Form Section -->
    <section class="subscription-form-section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-12">
                    <div class="subscription-container">
                        <div class="row g-5">
                            <!-- Plan Summary -->
                            <div class="col-lg-4">
                                <div class="plan-summary-card">
                                    <div class="plan-header">
                                        <div class="plan-badge">
                                            <span>{{ $plan->name }}</span>
                                        </div>
                                        <div class="plan-icon">
                                            <i class="ti ti-crown"></i>
                                        </div>
                                        <h3>{{ $plan->name }}</h3>
                                        <p class="plan-description">{{ $plan->description ?? 'Accès premium complet avec fonctionnalités exclusives' }}</p>
                                    </div>
                                    
                                    <div class="plan-pricing">
                                        <div class="price">
                                            <span class="currency">€</span>
                                            <span class="amount">{{ number_format($plan->price, 2) }}</span>
                                            <span class="period">/{{ ucfirst($plan->billing_cycle) }}</span>
                                        </div>
                                        <div class="price-note">Facturation automatique</div>
                                    </div>
                                    
                                    @if($plan->features && is_array($plan->features))
                                    <div class="plan-features">
                                        <h4>Fonctionnalités incluses</h4>
                                        <ul>
                                            @foreach($plan->features as $feature)
                                                <li>
                                                    <div class="feature-icon">
                                                        <i class="ti ti-check"></i>
                                                    </div>
                                                    <span>{{ $feature }}</span>
                                                </li>
                                            @endforeach
                                        </ul>
                                    </div>
                                    @else
                                    <div class="plan-features">
                                        <h4>Fonctionnalités incluses</h4>
                                        <ul>
                                            <li>
                                                <div class="feature-icon">
                                                    <i class="ti ti-check"></i>
                                                </div>
                                                <span>{{ __("lang.site_access_all_premium_content") }}</span>
                                            </li>
                                            <li>
                                                <div class="feature-icon">
                                                    <i class="ti ti-check"></i>
                                                </div>
                                                <span>Contenu exclusif en avant-première</span>
                                            </li>
                                            <li>
                                                <div class="feature-icon">
                                                    <i class="ti ti-check"></i>
                                                </div>
                                                <span>Support prioritaire</span>
                                            </li>
                                            <li>
                                                <div class="feature-icon">
                                                    <i class="ti ti-check"></i>
                                                </div>
                                                <span>Pas de publicités</span>
                                            </li>
                                        </ul>
                                    </div>
                                    @endif
                                    
                                    <div class="plan-footer">
                                        <div class="security-badge">
                                            <i class="ti ti-shield-check"></i>
                                            <span>Paiement 100% sécurisé</span>
                                        </div>
                                        <div class="guarantee-badge">
                                            <i class="ti ti-refresh"></i>
                                            <span>30 jours satisfait ou remboursé</span>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Payment Form -->
                            <div class="col-lg-8">
                                <div class="payment-form-card">
                                    <div class="card-header">
                                        <div class="header-icon">
                                            <i class="ti ti-credit-card"></i>
                                        </div>
                                        <h3>Finalisez votre Abonnement</h3>
                                        <p>{{ __("lang.site_fill_info_access_premium") }}</p>
                                    </div>
                                    
                                    <form id="subscriptionForm" action="{{ url('payment/stripe/subscription') }}" method="POST">
                                        @csrf
                                        <input type="hidden" name="plan_id" value="{{ $plan->id }}">
                                        <input type="hidden" name="amount" value="{{ $plan->price }}">
                                        <input type="hidden" name="currency" value="{{ $plan->currency ?? 'EUR' }}">
                                        <input type="hidden" name="payment_method" id="payment_method" value="stripe">

                                        <div class="form-section">
                                            <div class="section-header">
                                                <div class="section-icon">
                                                    <i class="ti ti-wallet"></i>
                                                </div>
                                                <h4>Méthode de Paiement</h4>
                                            </div>
                                            <div class="row g-3">
                                                <div class="col-12 d-flex gap-4 flex-wrap">
                                                    @if(\Helpers::isStripeEnabled())
                                                    <div class="form-check">
                                                        <input class="form-check-input" type="radio" name="gateway" id="gateway_stripe" value="stripe" checked>
                                                        <label class="form-check-label" for="gateway_stripe">
                                                            <i class="ti ti-brand-stripe me-1"></i> Carte (Stripe)
                                                        </label>
                                                    </div>
                                                    @endif
                                                    @if(\Helpers::isPayPalEnabled())
                                                    <div class="form-check">
                                                        <input class="form-check-input" type="radio" name="gateway" id="gateway_paypal" value="paypal" @if(!\Helpers::isStripeEnabled()) checked @endif>
                                                        <label class="form-check-label" for="gateway_paypal">
                                                            <i class="ti ti-brand-paypal me-1"></i> PayPal
                                                        </label>
                                                    </div>
                                                    @endif
                                                </div>
                                            </div>
                                        </div>

                                        <div class="form-section" id="card-section">
                                            <div class="section-header">
                                                <div class="section-icon">
                                                    <i class="ti ti-credit-card"></i>
                                                </div>
                                                <h4>Détails de la Carte</h4>
                                            </div>
                                            <div class="row g-3">
                                                <div class="col-12">
                                                    <div class="form-group">
                                                        <label for="card_number" class="form-label">
                                                            Numéro de Carte <span class="required">*</span>
                                                        </label>
                                                        <div class="input-wrapper">
                                                            <i class="ti ti-credit-card input-icon"></i>
                                                            <input type="text" class="form-control @error('card_number') is-invalid @enderror card-number" 
                                                                   id="card_number" name="card_number" 
                                                                   placeholder="1234 5678 9012 3456" required>
                                                        </div>
                                                        @error('card_number')
                                                            <div class="invalid-feedback">{{ $message }}</div>
                                                        @enderror
                                                    </div>
                                                </div>

                                                <div class="col-md-6">
                                                    <div class="form-group">
                                                        <label for="expiry" class="form-label">
                                                            Date d'Expiration <span class="required">*</span>
                                                        </label>
                                                        <div class="input-wrapper">
                                                            <i class="ti ti-calendar input-icon"></i>
                                                            <input type="text" class="form-control @error('expiry') is-invalid @enderror" 
                                                                   id="expiry" name="expiry" placeholder="MM/AA" required>
                                                        </div>
                                                        @error('expiry')
                                                            <div class="invalid-feedback">{{ $message }}</div>
                                                        @enderror
                                                    </div>
                                                </div>
                                                
                                                <div class="col-md-6">
                                                    <div class="form-group">
                                                        <label for="cvv" class="form-label">
                                                            CVV <span class="required">*</span>
                                                        </label>
                                                        <div class="input-wrapper">
                                                            <i class="ti ti-lock input-icon"></i>
                                                            <input type="text" class="form-control @error('cvv') is-invalid @enderror" 
                                                                   id="cvv" name="cvv" placeholder="123" required>
                                                        </div>
                                                        @error('cvv')
                                                            <div class="invalid-feedback">{{ $message }}</div>
                                                        @enderror
                                                    </div>
                                                </div>
                                            </div>
                                        </div>

                                        <div class="form-section">
                                            <div class="section-header">
                                                <div class="section-icon">
                                                    <i class="ti ti-settings"></i>
                                                </div>
                                                <h4>Options d'Abonnement</h4>
                                            </div>
                                            <div class="row g-3">
                                                <div class="col-12">
                                                    <div class="form-check custom-checkbox">
                                                        <input class="form-check-input" type="checkbox" id="auto_renew" name="auto_renew" value="1" checked>
                                                        <label class="form-check-label" for="auto_renew">
                                                            Renouvellement automatique de l'abonnement
                                                        </label>
                                                    </div>
                                                </div>
                                                
                                                <div class="col-12">
                                                    <div class="form-check custom-checkbox">
                                                        <input class="form-check-input" type="checkbox" id="terms" name="terms" value="1" required>
                                                        <label class="form-check-label" for="terms">
                                                            J'accepte les <a href="#" target="_blank">Conditions d'Utilisation</a> et la <a href="#" target="_blank">Politique de Confidentialité</a>
                                                        </label>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>

                                        <div class="form-actions">
                                            <button type="submit" class="btn btn-primary btn-subscribe" id="pay-button">
                                                <div class="btn-content">
                                                    <i class="ti ti-rocket"></i>
                                                    <span>Payer et s'abonner</span>
                                                </div>
                                                <div class="btn-loading" style="display: none;">
                                                    <i class="ti ti-loader ti-spin"></i>
                                                    <span>Traitement...</span>
                                                </div>
                                            </button>
                                            <div class="action-note">
                                                <i class="ti ti-info-circle"></i>
                                                <span>Votre abonnement sera actif immédiatement après le paiement</span>
                                            </div>
                                        </div>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Trust Section -->
    <section class="trust-section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8 text-center">
                    <h3>Pourquoi nous faire confiance ?</h3>
                    <div class="trust-grid">
                        <div class="trust-item">
                            <div class="trust-icon">
                                <i class="ti ti-shield-check"></i>
                            </div>
                            <h4>Sécurité</h4>
                            <p>Paiements cryptés et sécurisés</p>
                        </div>
                        <div class="trust-item">
                            <div class="trust-icon">
                                <i class="ti ti-headset"></i>
                            </div>
                            <h4>Support</h4>
                            <p>Assistance 24/7 disponible</p>
                        </div>
                        <div class="trust-item">
                            <div class="trust-icon">
                                <i class="ti ti-refresh"></i>
                            </div>
                            <h4>Garantie</h4>
                            <p>30 jours satisfait ou remboursé</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

<style>
.subscribe-page {
    background: var(--primary-color);
    min-height: 100vh;
    position: relative;
}

/* Hero Section */
.hero-section {
    padding: 140px 0 80px;
    background: var(--gradient-primary);
    position: relative;
    overflow: hidden;
}

.hero-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="heroPattern" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="75" cy="75" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="50" cy="10" r="0.5" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23heroPattern)"/></svg>');
    opacity: 0.3;
}

.hero-content {
    position: relative;
    z-index: 2;
}

.hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50px;
    padding: 0.75rem 1.5rem;
    margin-bottom: 2rem;
    color: white;
    font-weight: 600;
    font-size: 0.9rem;
}

.hero-badge i {
    color: #ffd700;
}

.hero-title {
    font-size: 3.5rem;
    font-weight: 900;
    color: var(--text-primary);
    margin-bottom: 1.5rem;
    line-height: 1.1;
}

.hero-title .highlight {
    background: var(--gradient-accent);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.hero-subtitle {
    font-size: 1.35rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin-bottom: 3rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
}

.hero-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    margin-top: 3rem;
}

.stat-item {
    text-align: center;
}

.stat-number {
    font-size: 2rem;
    font-weight: 800;
    color: var(--text-primary);
    margin-bottom: 0.5rem;
}

.stat-label {
    color: var(--text-secondary);
    font-size: 0.9rem;
    font-weight: 500;
}

.stat-divider {
    width: 1px;
    height: 40px;
    background: rgba(255, 255, 255, 0.2);
}

/* Floating Elements */
.floating-elements {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
}

.floating-card {
    position: absolute;
    width: 60px;
    height: 60px;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 1.5rem;
    animation: float 6s ease-in-out infinite;
}

.floating-card-1 {
    top: 20%;
    left: 10%;
    animation-delay: 0s;
}

.floating-card-2 {
    top: 60%;
    right: 15%;
    animation-delay: 2s;
}

.floating-card-3 {
    bottom: 20%;
    left: 20%;
    animation-delay: 4s;
}

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
}

/* Subscription Form Section */
.subscription-form-section {
    padding: 100px 0;
    background: var(--secondary-color);
    position: relative;
}

.subscription-form-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 100px;
    background: linear-gradient(to bottom, var(--gradient-primary), transparent);
}

.subscription-container {
    max-width: 1400px;
    margin: 0 auto;
    position: relative;
    z-index: 2;
}

/* Plan Summary Card */
.plan-summary-card {
    background: var(--card-bg);
    border-radius: 24px;
    padding: 2.5rem;
    border: 1px solid var(--border-color);
    height: 100%;
    position: sticky;
    top: 20px;
    box-shadow: 0 20px 40px rgba(0,0,0,0.1);
    transition: all 0.3s ease;
}

.plan-summary-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 30px 60px rgba(0,0,0,0.15);
}

.plan-header {
    text-align: center;
    margin-bottom: 2.5rem;
}

.plan-badge {
    display: inline-block;
    background: var(--gradient-accent);
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 50px;
    font-size: 0.8rem;
    font-weight: 600;
    margin-bottom: 1.5rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.plan-icon {
    width: 90px;
    height: 90px;
    background: var(--gradient-accent);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    box-shadow: 0 10px 30px rgba(15, 52, 96, 0.3);
}

.plan-icon i {
    font-size: 2.5rem;
    color: white;
}

.plan-header h3 {
    color: var(--text-primary);
    font-size: 1.75rem;
    font-weight: 800;
    margin-bottom: 1rem;
}

.plan-description {
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.6;
}

.plan-pricing {
    text-align: center;
    margin-bottom: 2.5rem;
    padding: 2rem;
    background: rgba(15, 52, 96, 0.05);
    border-radius: 20px;
    border: 1px solid rgba(15, 52, 96, 0.1);
}

.price {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 0.25rem;
    margin-bottom: 0.5rem;
}

.price .currency {
    font-size: 1.5rem;
    color: var(--text-secondary);
    font-weight: 600;
}

.price .amount {
    font-size: 3.5rem;
    font-weight: 900;
    color: var(--text-primary);
}

.price .period {
    font-size: 1.1rem;
    color: var(--text-secondary);
    font-weight: 500;
}

.price-note {
    color: var(--text-muted);
    font-size: 0.85rem;
    font-style: italic;
}

.plan-features {
    margin-bottom: 2.5rem;
}

.plan-features h4 {
    color: var(--text-primary);
    font-size: 1.2rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    text-align: center;
}

.plan-features ul {
    list-style: none;
    padding: 0;
    margin: 0;
}

.plan-features li {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 0;
    color: var(--text-secondary);
    border-bottom: 1px solid rgba(15, 52, 96, 0.1);
}

.plan-features li:last-child {
    border-bottom: none;
}

.feature-icon {
    width: 32px;
    height: 32px;
    background: var(--accent-color);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.feature-icon i {
    color: white;
    font-size: 1rem;
}

.plan-footer {
    text-align: center;
}

.security-badge, .guarantee-badge {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-bottom: 1rem;
}

.security-badge i, .guarantee-badge i {
    color: var(--accent-color);
    font-size: 1.1rem;
}

/* Payment Form Card */
.payment-form-card {
    background: var(--card-bg);
    border-radius: 24px;
    padding: 3rem;
    box-shadow: 0 20px 40px rgba(0,0,0,0.15);
    border: 1px solid var(--border-color);
}

.card-header {
    text-align: center;
    margin-bottom: 3rem;
    padding-bottom: 2rem;
    border-bottom: 2px solid var(--border-color);
}

.header-icon {
    width: 80px;
    height: 80px;
    background: var(--gradient-accent);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    box-shadow: 0 10px 30px rgba(15, 52, 96, 0.3);
}

.header-icon i {
    font-size: 2rem;
    color: white;
}

.card-header h3 {
    color: var(--text-primary);
    font-size: 2rem;
    font-weight: 800;
    margin-bottom: 1rem;
}

.card-header p {
    color: var(--text-secondary);
    margin: 0;
    font-size: 1.1rem;
}

/* Form Sections */
.form-section {
    margin-bottom: 2.5rem;
    padding: 2rem;
    background: rgba(15, 52, 96, 0.03);
    border-radius: 20px;
    border: 1px solid rgba(15, 52, 96, 0.08);
    transition: all 0.3s ease;
}

.form-section:hover {
    background: rgba(15, 52, 96, 0.05);
    border-color: rgba(15, 52, 96, 0.15);
}

.section-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 1.5rem;
}

.section-icon {
    width: 48px;
    height: 48px;
    background: var(--accent-color);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
}

.section-icon i {
    color: white;
    font-size: 1.25rem;
}

.section-header h4 {
    color: var(--text-primary);
    font-size: 1.35rem;
    font-weight: 700;
    margin: 0;
}

.form-group {
    margin-bottom: 1.5rem;
}

.form-label {
    color: var(--text-primary);
    font-weight: 600;
    margin-bottom: 0.75rem;
    display: block;
}

.required {
    color: #e74c3c;
    font-weight: 700;
}

.input-wrapper {
    position: relative;
}

.input-icon {
    position: absolute;
    left: 1rem;
    top: 50%;
    transform: translateY(-50%);
    color: var(--text-muted);
    font-size: 1.1rem;
    z-index: 2;
}

.form-control {
    background: var(--primary-color);
    border: 2px solid var(--border-color);
    color: var(--text-primary);
    border-radius: 16px;
    padding: 1rem 1rem 1rem 3rem;
    transition: all 0.3s ease;
    font-size: 1rem;
    width: 100%;
}

.form-control:focus {
    background: var(--primary-color);
    border-color: var(--accent-color);
    color: var(--text-primary);
    box-shadow: 0 0 0 0.25rem rgba(15, 52, 96, 0.15);
    outline: none;
}

.form-control::placeholder {
    color: var(--text-muted);
}

/* Custom Checkboxes */
.custom-checkbox {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
    margin-bottom: 1rem;
}

.custom-checkbox .form-check-input {
    margin-top: 0.25rem;
}

.custom-checkbox .form-check-label {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.5;
    cursor: pointer;
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
}

.custom-checkbox .form-check-label a {
    color: var(--accent-color);
    text-decoration: none;
    font-weight: 600;
}

.custom-checkbox .form-check-label a:hover {
    text-decoration: underline;
}

/* Styling des checkboxes */
.form-check {
    margin-bottom: 1rem;
    display: flex;
    align-items: center;
    gap: 0;
}

.form-check-input {
    width: 18px;
    height: 18px;
    margin: 0;
    margin-right: 0.75rem;
    background-color: var(--primary-color);
    border: 2px solid var(--border-color);
    border-radius: 4px;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    appearance: none;
    -webkit-appearance: none;
    -moz-appearance: none;
    flex-shrink: 0;
}

.form-check-input:checked {
    background-color: var(--accent-color);
    border-color: var(--accent-color);
}

.form-check-input:checked::after {
    content: '✓';
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: white;
    font-size: 12px;
    font-weight: bold;
}

.form-check-input:focus {
    outline: none;
    box-shadow: 0 0 0 0.2rem rgba(15, 52, 96, 0.25);
    border-color: var(--accent-color);
}

.form-check-input:hover {
    border-color: var(--accent-color);
}

.form-check-label {
    color: var(--text-secondary);
    font-size: 0.9rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    line-height: 1.4;
    margin-left: 0;
    padding-left: 0;
}

/* Form Actions */
.form-actions {
    text-align: center;
    margin-top: 3rem;
    padding-top: 2.5rem;
    border-top: 2px solid var(--border-color);
}

.btn-subscribe {
    background: var(--gradient-accent);
    border: none;
    color: white;
    padding: 1.25rem 3rem;
    font-size: 1.2rem;
    font-weight: 700;
    border-radius: 16px;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    min-width: 280px;
}

.btn-subscribe:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 30px rgba(15, 52, 96, 0.4);
}

.btn-subscribe:active {
    transform: translateY(-1px);
}

.btn-content, .btn-loading {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
}

.action-note {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    color: var(--text-muted);
    font-size: 0.9rem;
    margin-top: 1rem;
}

.action-note i {
    color: var(--accent-color);
}

/* Trust Section */
.trust-section {
    padding: 80px 0;
    background: var(--primary-color);
    text-align: center;
}

.trust-section h3 {
    color: var(--text-primary);
    font-size: 2rem;
    font-weight: 700;
    margin-bottom: 3rem;
}

.trust-grid {
    display: flex;
    justify-content: space-between;
    align-items: stretch;
    gap: 2rem;
    max-width: 1000px;
    margin: 0 auto;
}

.trust-item {
    flex: 1;
    padding: 2rem;
    background: var(--card-bg);
    border-radius: 20px;
    border: 1px solid var(--border-color);
    transition: all 0.3s ease;
    min-width: 0;
}

.trust-item:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 30px rgba(0,0,0,0.1);
}

.trust-icon {
    width: 64px;
    height: 64px;
    background: var(--gradient-accent);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
}

.trust-icon i {
    color: white;
    font-size: 1.75rem;
}

.trust-item h4 {
    color: var(--text-primary);
    font-size: 1.25rem;
    font-weight: 700;
    margin-bottom: 1rem;
}

.trust-item p {
    color: var(--text-secondary);
    margin: 0;
    line-height: 1.5;
}

/* Responsive Design */
@media (max-width: 1200px) {
    .hero-title {
        font-size: 3rem;
    }
    
    .plan-summary-card {
        position: static;
        margin-bottom: 2rem;
    }
}

@media (max-width: 768px) {
    .hero-section {
        padding: 100px 0 60px;
    }
    
    .hero-title {
        font-size: 2.5rem;
    }
    
    .hero-subtitle {
        font-size: 1.1rem;
    }
    
    .hero-stats {
        flex-direction: column;
        gap: 1.5rem;
    }
    
    .stat-divider {
        display: none;
    }
    
    .subscription-form-section {
        padding: 60px 0;
    }
    
    .payment-form-card {
        padding: 2rem;
    }
    
    .form-section {
        padding: 1.5rem;
    }
    
    .btn-subscribe {
        min-width: 100%;
        padding: 1rem 2rem;
    }
    
    .floating-card {
        display: none;
    }
}

@media (max-width: 576px) {
    .hero-title {
        font-size: 2rem;
    }
    
    .hero-subtitle {
        font-size: 1rem;
    }
    
    .plan-summary-card,
    .payment-form-card {
        padding: 1.5rem;
    }
    
    .trust-grid {
        flex-direction: column;
        gap: 1.5rem;
    }
    
    /* Amélioration des checkboxes sur mobile */
    .form-check-input {
        width: 20px;
        height: 20px;
        margin-top: 0.125rem;
    }
    
    .form-check-label {
        font-size: 0.85rem;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.getElementById('subscriptionForm');
    const cardSection = document.getElementById('card-section');
    const gatewayStripe = document.getElementById('gateway_stripe');
    const gatewayPayPal = document.getElementById('gateway_paypal');
    const paymentMethodInput = document.getElementById('payment_method');
    const payButton = document.getElementById('pay-button');

    function applyGateway(gateway) {
        if (gateway === 'paypal') {
            form.action = '{{ url('payment/paypal/subscription') }}';
            paymentMethodInput.value = 'paypal';
            if (cardSection) cardSection.style.display = 'none';
            payButton.querySelector('span').textContent = 'Continuer vers PayPal';
        } else {
            form.action = '{{ url('payment/stripe/subscription') }}';
            paymentMethodInput.value = 'stripe';
            if (cardSection) cardSection.style.display = 'block';
            payButton.querySelector('span').textContent = "S'abonner Maintenant";
        }
    }

    if (gatewayStripe && gatewayStripe.checked) applyGateway('stripe');
    if (gatewayPayPal && gatewayPayPal.checked && !gatewayStripe) applyGateway('paypal');

    if (gatewayStripe) gatewayStripe.addEventListener('change', () => applyGateway('stripe'));
    if (gatewayPayPal) gatewayPayPal.addEventListener('change', () => applyGateway('paypal'));

    // Card number formatting
    const cardNumber = document.getElementById('card_number');
    cardNumber.addEventListener('input', function(e) {
        let value = e.target.value.replace(/\s+/g, '').replace(/[^0-9]/gi, '');
        let formattedValue = value.match(/.{1,4}/g)?.join(' ') || value;
        e.target.value = formattedValue;
    });
    
    // Expiry date formatting
    const expiry = document.getElementById('expiry');
    expiry.addEventListener('input', function(e) {
        let value = e.target.value.replace(/\D/g, '');
        if (value.length >= 2) {
            value = value.substring(0, 2) + '/' + value.substring(2, 4);
        }
        e.target.value = value;
    });
    
    // CVV formatting
    const cvv = document.getElementById('cvv');
    cvv.addEventListener('input', function(e) {
        e.target.value = e.target.value.replace(/\D/g, '').substring(0, 4);
    });
    
    // Form submission
    form.addEventListener('submit', function(e) {
        const submitBtn = this.querySelector('.btn-subscribe');
        submitBtn.innerHTML = '<i class="ti ti-loader ti-spin me-2"></i>Traitement...';
        submitBtn.disabled = true;
    });
});
</script>
@endsection
